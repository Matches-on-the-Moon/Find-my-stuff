<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.motm"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="14" />

    <application
        android:name=".application.FMSApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/loginActivityLabel" />
        <activity
            android:name=".activities.RegisterActivity"
            android:label="@string/registerActivityLabel" />
        <activity
            android:name=".activities.FindItemActivity"
            android:label="@string/findItemActivityLabel" />
        <activity
            android:name=".activities.AddItemActivity"
            android:label="@string/addItemActivityLabel" />
        <activity
            android:name=".activities.ViewItemActivity"
            android:label="View an account" />
        <activity
            android:name=".activities.FindAccountActivity"
            android:label="Find an account" />
        <activity
            android:name=".activities.ViewAccountActivity"
            android:label="View an account" />
        <activity
            android:name=".activities.EditAccountActivity"
            android:label="Edit an account." />
        <activity
            android:name=".activities.EditItemActivity"
            android:label="Edit an item." />
    </application>

</manifest>